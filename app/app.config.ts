export default defineAppConfig({
  ui: {
    colors: {
      primary: 'green',
      secondary: 'teal',
      neutral: 'slate',
    },
    button: {
      defaultVariants: {
        // Rounded, friendly buttons
      },
    },
  },
})
