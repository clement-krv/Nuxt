# Connexion PostgreSQL
# En local (hors Docker) : lancez d'abord `docker compose up db`
DATABASE_URL=postgres://eco:eco@localhost:5433/ecotransport

# Secret de session (32 caractères minimum) — utilisé par nuxt-auth-utils
NUXT_SESSION_PASSWORD=change-me-super-secret-session-password-32chars
